name: Build and Upload Next.js App (SSR Ready)

on:
  push:
    branches:
      - main

jobs:
  build-and-upload:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Build project
        run: npm run build

      - name: Show contents of .next directory
        run: |
          echo "=== .next directory contents ==="
          ls -la .next || echo "'.next' directory does not exist"

      - name: Upload .next build output
        uses: actions/upload-artifact@v3
        with:
          name: nextjs-build
          path: .next
