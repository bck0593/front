name: Build and Deploy Next.js App to Azure

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Create .env.local
        run: |
          echo "NEXT_PUBLIC_API_URL=${{ secrets.FASTAPI_URL }}" > .env.local

      # 任意: テストやLintを実行したい場合のみ
      - name: Install dependencies (for test only)
        run: npm ci

      # 必要な本番ファイルだけをzipアーカイブ
      - name: Archive app for deployment
        run: |
          zip -r build.zip .env.local package.json package-lock.json next.config.js public src

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: app-001-step3-1-suzuyu-node-29
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE }}
          package: build.zip
